{
  "name": "monorepo",
  "version": "1.0.0",
  "description": "three-d-modules",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:a": "pnpm --filter @monorepo/project-a run dev",
    "dev:shared": "pnpm --filter @monorepo/shared run dev",
    "format": "pnpm prettier --write .",
    "format:check": "pnpm exec prettier --check .",
    "build": "./Dockerfiles/build.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.13.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "eslint-config-prettier": "^10.1.5",
    "prettier": "^3.6.2",
    "typescript": "~5.8.3"
  }
}
